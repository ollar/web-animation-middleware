{
  "name": "web-animation-middleware",
  "version": "1.0.0",
  "description": "A middleware wrapper over web animation api",
  "main": "dist/bundle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup src/index.js --output.format iife --output.file dist/bundle.js --name WAM -w"
  },
  "author": "ollar",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^0.55.5"
  },
  "keywords": ["web animation", "animation", "middleware"],
  "repository": {
    "type": "git",
    "url": "https://github.com/ollar/web-animation-middleware.git"
  },
  "bugs": {
    "url": "https://github.com/ollar/web-animation-middleware/issues"
  }
}
